# 道路联合核心V2实施记录

## 版本与安全边界

- 原分支：WH_heatOPT_li；起始提交：8648d1ab47a13cc6f44f9415b116c7cfd045468a。
- 不修改旧核心 `competition/core_model.py`，SHA-256：da9a29273e79a7c361fd8cd38b109023177500594168530942049a9812f8194d。
- 新模型命名为 road_joint_v2；旧结果、MPS与原始输入只读保留。
- 本次派生证据根目录：`runs/road_joint_v2/IMPLEMENT_20260830/`，不进入Git。
- 用户原有未跟踪 `docs/ai_review/` 保留，不纳入提交。

## 节点状态

- [x] 经济扩展包分类、读取校验、有效参数快照和缺口清单生成器。
- [ ] 道路原子化、支线几何校验和网络契约。
- [x] 独立V2核心及共享管段、三模式合成测试（不代表真实全季）。
- [x] 入口、标准结果、独立QA与Pareto接线（合成任务通过；真实入口被几何门禁阻止）。
- [ ] 新哈希、新任务计划和全季运行验收。

### 节点1：2026-08-30

内容：按稳定parameter_id读取41项数值，读取23项来源与25项待确认；严格解析布尔值、单位、状态、来源、重复ID、冬季月份与时段覆盖。保留412文件基线，4文件扩展独立校验，禁止通过放宽文件数量掩盖缺文件。新经济情景仅采用批准的8项设备参数，政策/燃气/历史管价只登记。

目的：避免空值变零、条件参数无条件覆盖能源时序、单位混淆和测试容量冒充真实DN。测试配置、有效值、来源与单位一起形成内容哈希；桌面缺口清单由此快照渲染，已有版本先备份。

验证：新测试先出现ModuleNotFoundError；实现后执行 `python -m pytest tests/test_road_v2_economics.py tests/test_guanggu_v03_intake.py -q`，37 passed。对授权真实目录运行现有完整校验器：426文件（416交付+10补丁）、62栋、2160小时、133920行，source/canonical均通过，守恒误差3.400e-16；model_ready=false、solver_executed=false。原始输入哈希校验通过。

经济快照：`runs/road_joint_v2/IMPLEMENT_20260830/economics/effective_parameters.json`。桌面清单：`C:/Users/leonl/Desktop/缺失数据清单.md`。

风险：经济快照准备命令本身只校验经济包，并不代替完整源数据与模型验收；尚未接入V2求解。报价、真实DN、工程走廊等仍非正式数据；旧核心与旧结果不受影响。

### 节点2：道路生成器已实现，真实接入存在决策阻塞

新增原子道路、唯一边ID、道路节点/站点关联、边界支线及几何门禁；不删除可行道路以减负。5项合成测试通过（去重、叶节点、可重复、角度、等级/桥隧过滤、遮挡失败等）。真实62栋检查中12栋在当前最近直线候选搜索中失败；不等于没有折线路径，详见独立失败报告。已向用户询问是否允许绕楼折线或需人工接入线。该节点未冒称62栋全部连通，不启动全季求解。

### 节点3：独立道路MILP及输出复算

新增 `competition/road_joint_v2/core.py`，不调用旧模型构建函数；只复用旧输入类型、字段验证和CRF函数。显式道路端口损失、每米泵耗、正反向互斥、3档容量、连通商品流、站点attachment供热；保留独立设备、COP/衰减、TES充放二进制/循环SOC、20%源侧容量裕度（含管损、排除TES）、成本与碳排表达式。

新增结果导出与独立QA：从导出的原始决策/逐时量和输入参数复算路口守恒、端口容量、设备容量、储热、成本、碳排；复算CRF直接使用定义式，不读取目标表达式代替验算。篡改成本或热流的负向测试会失败。

验证：核心13项测试通过；共享300m+两条10m支线案例中，主段中点流103.4kW、源供热106.4kW（100kW负荷+6.4kW损失），路由投资一次计930CNY，泵耗按米累计。相同输入下分布式成本/碳排与旧核心一致；混合固定接入可退化为两种纯模式。此次全量回归357 passed / 3 skipped，无失败；仅原依赖弃用警告。旧核心SHA不变。真实V2网络依然阻塞，不能将合成验收说成62栋全季完成。

### 节点4：唯一Builder、公共入口、任务和独立验收

新增 `builder.py` 将验收后的CanonicalSeasonData直接投影为不可变RoadCase；真实形状固定62栋×2160h，不调用旧案例的虚拟路径网络。继承v0.3逐时价格/碳因子，采用45℃设备曲线插值、低温容量修正、15℃上边界处理、LHV补丁与本次经济快照。

公共入口显式 `--core-version road_joint_v2`；未就绪时Python退出2，不构模/求解。真实入口再次验证426文件、62栋、2160h、133920行，通过后明确在12栋支线几何停止。桌面清单自动更新为44条（原25+额外7+几何12），旧清单先备份；不修改原件。

新任务系统固定6端点+三模式各11个ε点，首次全季只解锁集中成本端点。同一任务工作预留防止重复并发，成功结果按文件哈希校验，失败尝试留档；数学代码、输入、经济和网络变化后旧计划失效。合成验收使用5点，即21个完整小模型任务。每点独立新建V2模型，输出MPS、求解gap/最优界/内存记录及决策QA。

代表规则另有0.1%重算入口；政策上限未提供不捏造，提供时分别求三模式政策约束成本，不用网格近邻冒充最优。最低碳规则增加成本择优复核，消除同碳排下任意高投资容量。新ε上限不预加筛选容差，避免约束与筛选容差叠加造成虚假近竖直前沿；另以1e-6独立检查实际碳上限。

验证：5项Builder/门禁/任务测试通过，包括62×2160合成形状数据全行进入Builder、三模式21任务及代表复核、同逻辑任务并发拒绝、旧核心构建函数被mock禁止、输出篡改与case哈希变化拒绝。真实全季求解仍未启动。六张图从小模型决策实际生成并查看，不当作光谷地图或工程结果。

初次深路径全量复验出现3个Windows路径长度失败，保留于 `verification_final/`。短目录重跑362 passed / 3 skipped；最终证据收口于 `verification_signed/`（环境、全量测试、真实Python入口及426源文件哈希前后复验）。此前 `verification_short/`、`verification_release/`、`verification_complete/`、`verification_delivery/`及所有小案例输出均保留，不覆盖历史。最终负向哈希测试只修改独立副本，成功的小案例目录保持可复核，不留下故意被篡改的案例输入。

交付前增加4项静态结果篡改测试，先实证发现管容量/成本/摘要NaN和单边投资错值会被遗漏；修正QA仅豁免真正可空的DN/未建管寿命等字段，并校核单边初始投资、单价、CRF及年化投资。4项失败均转通过，核心/输出专项17 passed。没有改动核心数学方程。最终全量结果以`verification_signed/tests.log`为准。

最终实测：366 passed / 3 skipped，环境检查退出0；真实公共入口直接Python退出2，空间失败12栋，未构模/求解；426源文件哈希前后一致。最终小案例路径为 `runs/q_2a81a31a/test_task_guard_and_small_thre0/v2/`，21个扫描任务+7个代表复核子任务共28份QA全部通过；最大热平衡残差4.264e-14kW、费用残差1.422e-14CNY、碳排残差7.106e-15kgCO2e。该目录保留有效输入、任务、MPS、原始日志、GIS/调度及图表；负向篡改只发生在单独测试副本。

3项跳过来自旧 `tests/test_guanggu_v02_builder.py`：依赖的历史生成案例不完整/不存在；不是V2数学测试跳过。未用这3项作为V2或真实全季通过证据。

当前新核心文件SHA-256为`3191f4774e5feec521ebf639b3a6f63351e54981be39776a199aba83fbdf4d22`；实际任务还绑定全部competition数学相关代码、案例和源输入哈希，不能仅凭这一文件哈希复用任务。OSM快照SHA-256为`edff6c62d5d4096fbb05484b437c1ba4b8411d43dace2fc4ea2a50c4c45024c6`，读取前后相同。

操作与精确目录见 `docs/ROAD_JOINT_V2_RUNBOOK_CN.md`。本节点仅提交代码、测试和说明；桌面清单、所有结果和原始数据不提交；不推送远端。

### 未完成/阻塞，不能越过

1. 12栋的当前直线生成器未找到合法接入。需用户允许确定性避障折线，或提供人工接入线。不是“数据一定不可用”，也不是算力超时。
2. 真实原子网络未验收，故未生成真实V2冻结任务/MPS、未启动62栋×2160h成本端点，更没有39任务和最终全季Pareto成果。
3. 首个真实V2任务内存/耗时未测量，32核自动安全并发未验收；当前为独立单任务命令，不把旧核心并行器冒充新核心调度。
4. 正式设备报价、DN、管损/泵耗/站房/接入/TES等仍含假设。测试价格和空间结果只能验证软件数学实现。
5. 真实政策碳约束尚未给值；四代表规则不保证四个不同方案，不能为了凑数生成伪结果。
