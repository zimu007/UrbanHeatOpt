# 规划道路与多候选接入：本轮验收说明

日期：2026-08-30。代码节点：e898825、7e47189；分支WH_heatOPT_li。未推送远端，结果和真实数据不提交Git。

## 结论

按“源荷匹配与规划优化，不做施工图设计”完成代码修订。取消仅主次干路、禁止跨道路、45°限制；每栋最多3个接入候选，由同一V2优化器选择。施工审查、埋深、沟槽等不再构成本轮运行门槛。

**62栋全季优化尚未完成。** 真实输入和空间准备已经自动通过；本机完整构模过程中触发内存安全预算，尚未进入HiGHS，未生成MPS、求解结果或全季Pareto。不能把本次资源停止说成模型不可行，也不能说32GB物理内存已经耗尽。

## 本次新证据

| 阶段 | 实测结果 |
|---|---|
| 修改前环境/全量测试 | 环境通过；366 passed、3 skipped |
| 失败测试 | 网络新增6项先失败；接入选择新增6项先失败，分别保留red日志 |
| 最终环境/全量测试 | 环境通过；385 passed、3 skipped、无失败 |
| 自动源校验与标准化 | 426个有效交付/补丁文件；62栋×2160小时=133920行 |
| 时间 | 2021-12-01 00:00+08:00至2022-02-28 23:00+08:00，按源数据基准年跨年重建，并非运行日期 |
| 空间与接入 | 63栋障碍、62栋负荷；399节点、578物理边、5候选站；61栋各3候选、1栋1候选，共184个 |
| 全流程生成器实际最长候选 | 233.3286米；以生产数据及障碍筛选结果为准，之前只读简化诊断的231.36米不是冻结长度 |
| 小案例 | 真实生成算法的多候选小案例三模式21任务；历史共享管段小案例21任务+7代表复核；受控单任务1个，共50份成功QA |
| 独立QA最大误差（上述合成任务） | 热平衡4.2633e-14kW；费用1.4211e-14CNY；碳排7.1054e-15kgCO2e |
| 新多候选单线程重复运行 | 另建两个独立任务，比较12份CSV/Parquet结果，最大数值差异0（≤1e-9）；仅1栋×2h合成验收 |
| 真实全季任务 | 62栋、2160小时、8线程、1% gap、求解器21600秒配置；约244.406秒触发内存保护 |
| 数据/版本完整性 | 校验及全季尝试后源/模型哈希一致；旧core_model.py SHA-256未变 |

三项跳过是既有历史回归fixture缺失，不是本轮V2或真实输入校验跳过。42条警告主要为既有依赖弃用警告，详见完整日志。

## 资源停止的准确含义

- 任务启动时可用内存：17234432000 bytes，约16.05 GiB。
- 自动预算：12925824000 bytes，约12.04 GiB，即当时可用内存75%。
- 停止时进程私有已提交内存：12937949184 bytes，约12.05 GiB；工作集9115873280 bytes，约8.49 GiB。
- 当时系统仍有9019248640 bytes可用内存，约8.40 GiB；监控在可能进一步增长前停止自己启动的进程。
- failure.json为ResourceSafetyStop / memory_budget_exceeded，model_build_completed=false。没有MPS、solver_evidence、success或优化决策文件。
- 按当前变量定义及578边、3档、2160小时推算，完整模型约9209235个变量，其中7490880个正反向管流变量、1248480个逐时方向二进制变量。**这是代码解析的规模计算，不是已完成构模的实测nvariables。**
- 目前不能给出整个构模、MPS导出和求解阶段的最终峰值内存，也不能仅凭“32核”判断服务器能否运行。需要更充足的可用内存后重试；本轮未通过削减时段、管段或TES规避规模。

## 输出和复现

本轮总索引：`runs/road_joint_v2/PLANNING_20260830/INDEX.md`。

- `verification/verification_evidence.json`：本次环境、全量测试、真实入口命令、耗时、退出码和源哈希完整性。
- `verification/real_input_gate/preparation_status.json`：source/canonical/spatial/model_ready为true；准备阶段solver_executed=false。
- `verification/real_input_gate/spatial/preview/`：实际渲染并查看的候选地图，明确标注“不是优化建设结果”。
- `verification/real_input_gate/case.json`、`task_plan.json`：新输入和39个完整全季任务，不沿用旧任务。
- `verification/real_input_gate/tasks/central-cost/attempt_0001/`：本次完整构模尝试及安全停止证据。
- `verification/real_input_gate/resource_monitor/central-cost/20260830T093401804711Z/`：精确命令、122次内存采样、日志和摘要。
- 最终测试产物：`runs/q_00e6ed57/`。其中multichoice为1栋×2h合成路网，v2为2栋×2h共享路段案例；均不得代替真实全季成果。
- 桌面`缺失数据清单.md`由本次有效参数自动生成，旧文件已备份；现在32项，不再包含已解除的12栋几何接入缺口。真实报价等影响结果的经济解释力，不作为施工级门槛。

在更多可用内存且相同代码/输入的环境中，从仓库根目录重试：

```powershell
conda run --no-capture-output -n urbanheatopt_env python scripts/run_road_v2_guarded.py `
  --run-root 'runs/road_joint_v2/PLANNING_20260830/verification/real_input_gate' `
  --task-id central-cost
```

相同任务重试会创建新attempt并保留旧证据；不同机器路径、代码或输入发生改变时，按`docs/ROAD_JOINT_V2_RUNBOOK_CN.md`重新prepare生成新RUN_ID，不手工修改旧任务哈希。只有首端点经过认证gap和独立QA，才解锁其余端点及33个ε任务。

## 尚未完成

- 真实全季完整构模、MPS导出、六端点及33个ε任务、代表解复核与全季独立QA：受本次资源安全预算阻塞。
- 完整全季重复运行：没有首个成功结果，不能声称可复现已通过。
- 政策碳约束代表解：真实政策上限未给定，不编造；不影响无该政策的前沿计算。
- 施工可实施性、真实DN水力和施工资料：不属于本轮比赛验收目标，不作为当前缺陷或阻塞。

后续重点是提供服务器**总内存与可用内存**、保持当前完整模型和输入重新运行，而非补施工图资料。
