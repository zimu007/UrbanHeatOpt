# UrbanHeatOpt 代码组任务书与负荷组协作接口

> 项目：CP-202611“面向供热格局变革的城镇供热源荷匹配与规划寻优模型构建”  
> 赛题方向：夏热冬冷地区高品质供热系统的新建场景  
> 实施区域：光谷软件园（流程试点）→ 光谷数字经济产业园 / 武汉新城核心区（最终场景）  
> 文档版本：v1.0，2026-08-04  
> 本文档同时面向代码组成员和代码组使用的 Codex。

---

## 0. 如何使用本文档

- 代码组负责人先阅读第 1～5 节，确认阶段目标和任务优先级。
- 数据接口开发者重点执行第 6～8 节。
- 优化模型开发者重点执行第 9～11 节。
- 每完成一个任务，将对应的 `- [ ]` 改为 `- [x]`，并在其后附提交号、输出路径或测试结果。
- 代码组的 Codex 每次只处理一个可验收任务；完成代码、测试和文档后再进入下一项。
- 未经来源确认的数据不得被包装成“真实参数”；暂定值必须进入假设清单和敏感性分析。

---

## 1. 对“原代码输入变量缺失”的正式回应

### 1.1 结论

代码组最初的观察**基本正确，但表述需要更准确**：

1. 当前下载的仓库并不包含完整的 Fehring 示例输入，因此**无法原样复算作者示例的数值结果**。
2. 这不等于优化模型的核心变量或公式被删除。数据读取接口、默认参数模板、聚类逻辑、Pyomo 优化模型和示例结果图仍然存在。
3. 缥缈地寻找“被屏蔽的原始变量”不是当前重点。代码组应根据现有接口建立一套**可验证、可追溯的武汉案例输入**，先复现方法流程，再针对比赛要求扩展模型。

因此应区分：

- **复现作者 Fehring 案例的精确结果**：目前做不到，缺少原始案例数据和运行参数快照。
- **复现 UrbanHeatOpt 的完整工作流**：可以做到，需要补齐合法输入并修复若干接口和可复现性问题。
- **形成适合本竞赛的武汉新建场景模型**：可以做到，但不能停留在原代码，需要增加设备性能、碳排目标、负荷柔性和更合理的管网候选方案。

### 1.2 已核实的证据

| 现象 | 代码或文件证据 | 对项目的影响 |
|---|---|---|
| 案例数据未随仓库提供 | [`.gitignore`](./.gitignore) 全局排除了 `*.csv`、`*.geojson`、`*.xlsx`、`*.shp` 等数据格式 | Fehring 的 `Building_Data.geojson`、`Building_TS.csv` 和场景输入未被纳入下载包 |
| Fehring 目录不完整 | 当前仅保留少量结果图和 `Cost_Parameter.json` | 历史结果只能参考，不能视为可复现实验 |
| 默认模板仍在 | [`default/`](./default/) 内保留参数、热源、余热曲线、气象和建筑类型模板 | 可以据此推断接口并新建案例 |
| Notebook 有历史输出 | [`main.ipynb`](./main.ipynb) 以 Fehring 为示例并保存了大量历史输出 | “曾经运行成功”不代表当前文件包可从头运行 |
| 负荷存在隐式换算 | [`clustering.py`](./clustering.py) 的 `cluster_heat_demand()` 将输入除以 `1e3` | 如果负荷组直接提供 kW，结果会被错误缩小 1000 倍 |
| 原始求解目标只有成本 | [`model.py`](./model.py) 的目标函数只汇总投资和运行成本 | 尚不满足成本—碳排多目标比较 |

### 1.3 当前工作原则

> 不把“精确复算 Fehring”作为前置条件；用合成最小样例验证程序，再用负荷组提供的武汉数据建立正式案例。

---

## 2. 代码组的阶段目标

### 2.1 当前阶段：光谷软件园试点

在光谷软件园构建一条可重复执行的数据链：

```mermaid
flowchart LR
    A[负荷组标准数据] --> B[输入校验器]
    B --> C[UrbanHeatOpt 适配器]
    C --> D[建筑聚类]
    D --> E[管网候选生成]
    E --> F[源-网-荷-储优化]
    F --> G[结果校验与可视化]
    G --> H[方案比较报告]
```

本阶段的成功标准不是“图画出来了”，而是：

- 从原始交付到结果可以由脚本一键重现；
- 数据单位、ID、时间索引和坐标系都有显式检查；
- 至少得到一个小规模可解释的优化解；
- 能说明每一项投资选择为何发生；
- 同一输入、同一随机种子得到相同结果。

### 2.2 最终阶段：光谷数字经济产业园 / 武汉新城核心区

形成适用于新建场景的源—网—荷—储协同规划模型，至少比较：

1. 全分散供热；
2. 组团级能源站 / 微网；
3. 区域集中供热；
4. 在上述方案中配置热泵、余热、储热及需求响应的组合方案。

最终成果需给出成本、碳排、供热可靠性、舒适性和空间方案，而不只是一个求解器目标值。

### 2.3 当前不做的事情

- 不继续猜测或逆向恢复 Fehring 原始数据。
- 不把 Notebook 中保存的旧输出当作本项目运行证据。
- 不用未经授权的百度或 Google 地图抓取结果构建生产数据。
- 不在输入单位未冻结前进行大规模求解。
- 不一开始就对武汉新城全域建模；先用光谷软件园跑通最小闭环。

---

## 3. 当前仓库需要立即处理的问题

按优先级分为 P0、P1、P2。P0 未完成前不得宣称“流程已跑通”。

### 3.1 P0：会直接导致错误结果或无法复现

- [x] **建立版本管理基线**：当前 `UrbanHeatOpt` 文件夹不是 Git 仓库。执行 `git init`，先提交一份未经功能修改的 `upstream_snapshot`，再建立竞赛开发分支。
  - 完成记录（2026-08-10）：复用现有 Git 仓库和竞赛开发分支 `WH_heatOPT_li`，未重复执行 `git init`；将上游提交 `e385220b80e86f8cc9b2006f6dec6eb082126ae3` 登记为带注释标签 `upstream_snapshot`，并验证该基线为当前分支祖先。
- [x] **重写数据忽略规则**：取消 `.gitignore` 对所有 CSV、GeoJSON、XLSX 的无差别排除。大文件和原始数据可忽略，但必须允许提交 `tests/fixtures/` 下的小型合成样例、数据字典和校验结果。
  - 完成记录（2026-08-11）：改为按 `cases/**/raw`、临时工作区、结果和日志目录排除派生或正式数据；允许 `tests/fixtures/` 中的小型 CSV、GeoJSON、XLSX、Parquet、数据字典和校验基准进入版本管理。同步修正 `.gitattributes`，避免二进制文件被通用文本规则覆盖；已通过忽略规则、文件属性、二进制哈希和空白错误检查。
- [x] **冻结单位契约**：明确建筑负荷的输入单位为 `kW`。在适配器边界完成一次换算，并删除或参数化 `clustering.py` 中隐式 `/1e3`。
  - 完成记录（2026-08-11）：标准负荷、兼容宽表和聚类结果统一为浮点 kW；删除生成器 `×1000/uint32`、聚类 `/1000` 及年度对账中的千倍假设。4 栋 × 24 小时内存合成数据经长表、宽表和聚类累计守恒测试通过。
- [x] **冻结时间索引**：标准数据使用 `timestamp`；当前 UrbanHeatOpt 兼容文件使用 `hour=1...N`。不得混用 `0...8759` 和 `1...8760`。
  - 完成记录（2026-08-11）：新增 Asia/Shanghai 整点连续时间校验和稳定 `timestamp → hour=1...N` 双射；拒绝 0 起点、缺号、重复、倒序、浮点 hour、错误时区、非整点和跨建筑覆盖不一致。
- [x] **增加输入校验器**：在聚类和求解前检查文件、字段、单位、ID、时间、CRS、空值和非负性，错误时立即停止并给出可读提示。
  - 完成记录（2026-08-14）：新增 `competition/validation/inputs.py` 与 `scripts/validate_inputs.py`，在适配、聚类和求解前校验 `case_config.yaml`、建筑 GeoJSON、逐时负荷 Parquet、设备表、道路/可建设空间和外部时序；覆盖字段缺失、ID 不一致、时间范围、小时连续性、单位契约、CRS、几何有效性、空值、非负性、P0 技术类型和跨文件 `data_version`/DHW 一致性。验证命令：`python scripts/validate_inputs.py --case tests/fixtures/minimal_case` 与 `python -m pytest -q tests/test_validate_and_adapt_case.py tests/test_contract_schema.py tests/test_load_unit_time_contract.py`。
  - 合并增强（2026-08-15）：保持 `CaseInputs` 和 `validate_case_inputs()` API 不变，增加 YAML 重复键拒绝、稳定错误码、契约输入 SHA-256、校验前后全文件快照只读检查，以及 ID/单位/字段不自动修正测试；CLI 输出 JSON 摘要并以退出码 2 表示契约失败。
- [x] **建立最小合成样例**：3～6 栋建筑、24～48 小时、1 个热源、可选 1 个储热设备；所有输入均可提交到 Git。
  - 完成记录（2026-08-14）：确认并补正文档化的 `tests/fixtures/minimal_case/`，数据版本为 `synthetic-v1`，包含 4 栋建筑、24 个逐小时步长、1 个 `synthetic_fixed_source` 合成固定热源、无储热、无余热；输入文件包括 `case_config.yaml`、`buildings.geojson`、`building_hourly_loads.parquet`、`technologies.csv`、`roads_or_feasible_space.geojson`、`external_timeseries.parquet` 和 README，均位于可提交的测试夹具目录。验证命令：`python scripts/validate_inputs.py --case tests/fixtures/minimal_case` 通过，报告 `buildings=4`、`hours=24`、`technologies=1`；`python -m pytest -q tests/test_validate_and_adapt_case.py` 通过，结果为 6 passed、11 warnings。该样例仅为 `synthetic_test`，不使用、不修改、不替代负荷组真实数据。
- [x] **确定可移植求解器**：当前环境已安装 `urbanheatopt_env`，`appsi_highs` 可用；配置中应写 `solver: highs`。`model.py` 只识别 `highs` 或 `gurobi`，不要在 YAML 中写 `appsi_highs`。
  - 完成记录（2026-08-11）：默认配置改为 `highs`，固定单线程、60 秒时限和随机种子；`model.py` 在求解前校验配置与可用性，显式映射内部 `appsi_highs`，只在终止状态为 `optimal` 时加载变量。非法名称、不可用 Gurobi 和不可行模型均在导出前失败，15 项求解器接口测试通过。
- [x] **添加自动测试**：至少覆盖读取、单位换算、建筑聚类、负荷守恒和最小模型求解。
  - 完成记录（2026-08-15）：现有测试覆盖标准案例读取、kW 浮点保持与禁止隐式千倍换算、聚类严格求和守恒、`timestamp→hour=1...N`、HiGHS 最小模型最优/不可行门禁；新增重复 YAML 键、SHA-256、源文件只读、错误 ID/单位/字段不自动修正和 `FEATURE_NOT_IMPLEMENTED` 稳定错误码测试。完整测试套件共65项通过。

### 3.2 P1：当前代码中的高风险逻辑

- [ ] 将 `read_geo_data_from_disk()` 的全表 `dropna()` 改为只检查模型必需字段；否则任何辅助字段为空都会误删整栋建筑。
- [ ] 为 KMeans、温度设定、运行时程等随机过程设置并记录 `random_seed`。
- [ ] 修正生活热水天数 `356`；同时增加 `include_dhw` 开关，禁止在负荷组已经包含生活热水时再次叠加。
- [ ] 修正无余热场景：当前 `save_data()` 会对 `None` 调用 `to_csv()`，应允许没有余热源和余热曲线。
- [ ] 修正余热曲线检查：当前函数在检查第一个余热源后就返回，应逐个核验全部余热源。
- [ ] 校验 `param_cluster_size <= 建筑数量`，并处理少于 3 个非共线节点时 Delaunay 无法建立的问题。
- [ ] 将模型中的热源曲线统一为热功率 `kW`；当前余热曲线实际被当作体积流量，再乘以水温差和比热，容易产生单位误解。
- [ ] 读取并使用储热损失率；当前 `pTESlosses` 被硬编码为 `0.0`。
  - V0 新核心记录（2026-08-21）：竞赛 Core 已从 V3 技术表读取逐时损失率，并在循环 SOC 平衡中使用；legacy `model.py` 仍保留旧硬编码，因此原代码风险项保持未勾选。
- [ ] 检查所有小时的未供热量 `vHNS`，最终正式方案原则上应为 0；不能只依赖高罚值掩盖供能不足。

### 3.3 P2：竞赛所需能力扩展

- [ ] 增加碳排核算与成本—碳排 Pareto 求解。
  - V0 完成记录（2026-08-21）：新竞赛 Core 已核算中央/分布式热泵购电和燃气锅炉 LHV 燃气的年度运行物理碳排；三种模式分别执行成本端点、碳端点和 epsilon-constraint 扫描，再形成合并非支配前沿。泵耗碳排、正式供暖季和正式参数尚未接入，故总任务保持未勾选。
- [ ] 将“所有可调热源都视为 boiler”的简化结构升级为技术类型表。
  - V0 完成记录（2026-08-21）：新竞赛 Core 已按独立 `technology_id` 区分中央空气源热泵、中央燃气锅炉、分布式空气源热泵和水蓄热；legacy 模型仍保留原结构，正式入口不再调用它。正式技术全集尚未完成，故保持未勾选。
- [ ] 支持空气源热泵随室外温度、供水温度和负荷率变化的 COP/容量曲线。
  - 接口记录（2026-08-21）：已定义并接通逐技术逐时 `cop` 与 `capacity_ratio` Provider，Core 使用其约束容量和核算购电；正式温度插值/外推算法尚待负责人交付，`v1-full` 禁止回退固定 COP，故保持未勾选。
- [ ] 支持水源/地源热泵、低品位余热热泵、电锅炉、燃气锅炉和储热。
- [ ] 支持分时电价、能源价格和电力碳排因子的逐时序列。
  - V0 接入记录（2026-08-23）：新增可追溯 `provisional_v0` profile，按用户确认的七段绝对电价生成逐时时序；将 `3.54 CNY/Nm³` 按国家缺省 `38.931 MJ/Nm³` 在边界一次换算为 `0.327348386 CNY/kWh_LHV`，并接入湖北电力因子与天然气直接燃烧 CO₂ 因子。正式项目价格和碳因子未签认，因此总任务保持未勾选。
- [ ] 增加需求响应或室温约束；原模型只有固定热负荷，没有室温状态。
- [ ] 将直线 Delaunay/MST 管网升级为沿道路候选通道的最短路径网络；MST 仅保留为基线。
  - 接口记录（2026-08-21）：已冻结 `CandidateNetworkProvider` 返回候选站和物理无向管段的接口；自动生成与多候选站选址尚未交付，正式主线遇到 `candidate_source=generate` 仍明确失败。
  - V0 基线记录（2026-08-23）：已对光谷 v0.2 实现唯一负荷加权中心站和欧氏 MST，固定投影、排序、并列处理和 ID；所有产物明确标为 `provisional_geometric_mst`、非道路约束且未验证施工可实施性。道路候选与选址优化仍未实现，故总任务保持未勾选。
- [ ] 增加管网热损失、泵耗和供回水温度情景。
- [ ] 支持代表日/代表周优化，并用完整供暖季逐时回放校核投资方案。
  - 发布门记录（2026-08-21）：新增只读 `scripts/check_v1_release_gate.py`，强制检查冻结 3.0.0 契约、2160 个权重为 1 的小时、正式能力状态和全部 Pareto QA。当前真实数据及外部物理模块未交付，任务保持未勾选且程序不得命名为 V1.0。

---

## 4. 建议的开发目录

不大规模搬动原作者代码，先在仓库中增加竞赛层：

```text
UrbanHeatOpt/
├── competition/
│   ├── adapters/                 # 负荷组数据 → UrbanHeatOpt
│   ├── schemas/                  # 字段、单位和 JSON Schema/Pandera 规则
│   ├── configs/                  # 软件园及最终场景配置
│   ├── pipelines/                # 数据准备、求解、后处理入口
│   └── reports/                  # 自动生成的 QA 与结果摘要
├── cases/
│   ├── guanggu_software_park/    # 试点案例；原始大数据可不进 Git
│   └── wuhan_new_city/           # 最终案例
├── scripts/
│   ├── validate_inputs.py
│   ├── adapt_load_team_data.py
│   ├── run_case.py
│   └── compare_scenarios.py
├── tests/
│   ├── fixtures/minimal_case/    # 必须可随仓库分发
│   └── test_*.py
├── docs/
│   ├── DATA_CONTRACT.md
│   ├── MODEL_ASSUMPTIONS.md
│   └── CHANGELOG_COMPETITION.md
└── CODE_TEAM_ROADMAP.md
```

前期准备记录（2026-08-11，状态更新至2026-08-15）：已建立 `competition/` 五个竞赛层子包、两个真实案例边界目录、`scripts/`、`tests/fixtures/minimal_case/` 及职责说明；随后新增 `DATA_CONTRACT.md`、`MODEL_ASSUMPTIONS.md`、问题清单和真实数据暂缓清单。当前已实现运行层完整输入校验、最小合成案例、旧模型适配、路径式 CLI、标准结果结构和成本年化基础；负荷组9文件正式接收、真实案例对账与完整优化闭环仍未完成。未移动或删除任何原作者目录。

要求：原作者文件的功能修改应小步进行，并在 `CHANGELOG_COMPETITION.md` 中记录“修改原因、公式影响和测试证据”。

---

## 5. 推荐实施顺序

### 工作包 A：建立可运行基线

交付物：环境检查报告、最小样例、自动化运行命令。

- [x] 记录 Python 和主要依赖版本。
  - 完成记录（2026-08-11）：新增 `scripts/check_environment.py` 和 `docs/ENVIRONMENT_REPORT.md`；确认现有 `urbanheatopt_env` 的固定版本、conda-forge CPU PyArrow、带时区 Parquet 往返及 APPSI HiGHS 最小求解均通过。全新环境仅完成 dry-run，尚未实际重建。
- [x] 将默认求解器设为 `highs`，同时保留可选 Gurobi 配置。
  - 完成记录（2026-08-11）：`_config.yaml` 默认使用 `highs`；保留显式 `gurobi` 选择，但不可用时可读失败且不静默回退。HiGHS 一变量最优解、不可行门禁和参数传递测试通过；完整案例求解仍待最小合成案例任务。
- [x] 建立非 Notebook 单命令入口，避免把 Notebook 当作唯一入口。
  - 完成记录（2026-08-21）：按 V3 主线约定使用 `python scripts/run_case.py --case tests/fixtures/v3_smoke_case --profile v0-smoke`；入口只调用统一 Pipeline 和新 Pyomo Core，不静默回退 `model.run_model()`。旧契约仅允许显式 `scripts/run_legacy_case.py` 回归。
- [ ] 最小案例完整执行：读取 → 聚类 → 管网 → 优化 → 导出。
  - V0 阶段记录（2026-08-21）：2 节点×24 小时 V3 合成案例已完成读取、给定候选网、三模式优化、Pareto、标准导出和 QA；本例直接消费需求节点和给定候选站/管网，未执行建筑聚类或自动候选生成，因此保持未勾选。
- [ ] 输出求解状态、目标值、未供热量、负荷平衡误差和运行时间。
- [x] 在 CI 或本机测试命令中重复运行两次，确认结果一致。
  - 完成记录（2026-08-21）：`tests/test_v3_smoke_pipeline.py` 从同一只读输入分别运行两次完整 V0 Pipeline，Pareto 点按稳定键比较，数值绝对误差不超过 `1e-9`，并确认全部输入 SHA-256 不变。

### 工作包 B：负荷组数据适配

交付物：数据校验器、转换器、接口说明和最小交接样例。

- [x] 建立光谷 v0.3 全量源清单和自主只读接收校验。
  - 完成记录（2026-08-23）：新增独立 `guanggu_v03` profile，不覆盖历史
    `wuhan_v02`；递归识别并哈希 412 个文件，实际读取 369 个 DeST CSV、
    4 个工作簿及全部标准/追溯文件，独立重算 62 栋、543120 行、供暖季
    映射、ID/面积/几何/时序/设备/LHV/三方案哈希和 Building_TS 对账。
    本节点只完成源接收校验，尚未因此宣称标准化、模型就绪或求解完成。
- [x] 将光谷 v0.3 完整供暖季适配为不可变标准数据快照。
  - 完成记录（2026-08-23）：按 `8016..8759 → 0..1415` 选取并重排为
    `hour=1..2160`，生成62栋×2160小时（133920行）标准负荷、跨年时间双射、
    外部时序、技术参数登记表和设备曲线；体积气价/碳因子只在边界按
    38.931 MJ/Nm³ 转为 LHV 能量口径，22个高于15℃小时显式封顶。
    该快照不包含道路、管径、储热和完整经济输入，不能据此宣称模型就绪。
- [x] 建立光谷 v0.3 模型就绪门禁和自动缺失项报告。
  - 完成记录（2026-08-23）：校验命令分别输出源校验、标准数据复验、
    `model_ready` 和 `solver_executed`，并从同一机器结果生成桌面中文说明；
    正式入口在必需空间、设备、储热、经济和连接层缺失时以退出码2停止，
    自动测试证明不会实例化求解器或调用新旧求解 Pipeline。
- [x] 完成光谷 v0.3 输入全流程自动验收和操作交付。
  - 完成记录（2026-08-23）：公开命令自动完成412文件分类/读取/哈希、62栋
    全年负荷与外部时序校验、2160小时适配、标准复验和模型门禁；桌面说明由
    机器结果生成，列出22个COP封顶小时、数据字典、全部文件哈希、详细缺失项
    和VS Code复现步骤。该项只验收输入闭环，`model_ready=false`、
    `solver_executed=false`；最终专项18项、全仓269项测试通过，未勾选完整
    供暖季模型运行。

- [x] 建立光谷 v0.2 全量源清单和只读接收校验。
  - 完成记录（2026-08-23）：新增 `wuhan_v02` profile 和专用只读校验器；
    全量识别、哈希 394 个文件，解析 369 个 DeST CSV，并验证 03/04/05 的
    行数、字段、CRS、建筑 ID、时区、逐栋 `0..8759` 小时覆盖和非负负荷。
    工作簿仅作来源追溯并产生非阻断警告；尚未完成到新 Core 的字段适配。
- [x] 读取负荷组的标准长表与建筑清单。
  - 完成记录（2026-08-23）：`adapt_wuhan_v02_sources()` 读取 03 建筑 GeoJSON、
    04 原型映射和 05 建筑逐时负荷，将 62 栋×8760 小时输出为 draft.2 标准
    建筑、原型映射和长表；不修改源文件、不重复叠加新风负荷。
- [x] 检查所有建筑 ID 是否一一对应。
  - 完成记录（2026-08-23）：接收层和适配层同时检查 03/04/05 ID 集合；
    单一用途生成一个标准分区，6 栋混合用途保留多分区，分区面积对账误差
    不超过 0.1%。
- [x] 将光谷 v0.2 自动准备为新 Core 的 V0 峰值日输入。
  - 完成记录（2026-08-23）：全量 62 栋×8760 小时先通过只读校验，再按年度供暖量稳定选择前 8 栋，并截取全园区总负荷峰值自然日 24 小时；正式入口直接调用统一 Core，集中、分布、混合三模式实测均为 `optimal`，未调用 legacy 模型。
- [ ] 从长表生成 UrbanHeatOpt 所需宽表。
- [ ] 由建筑属性生成或补充 `Building_Data.geojson` 的模型字段。
- [ ] 生成“转换前后总热量、峰值和建筑数量”对账表。
- [ ] 保留原始文件只读副本；所有清洗和换算必须通过脚本完成。

### 工作包 C：光谷软件园试点

交付物：软件园案例目录、基线结果、错误清单和一页结论图。

- [x] 完成光谷 v0.2 的 8 栋峰值日 V0 自动闭环与独立 QA。
  - 完成记录（2026-08-23）：公开命令先只读审计并标准化 62 栋×8760 小时，再按年度供暖量稳定选择前 8 栋和全园区峰值自然日 `2021-01-08`；统一新 Core 的集中、分布、混合三模式均为 `optimal`，epsilon-constraint 共保留 13 个非重复非支配点，全部通过热平衡、连通、储热、成本、碳排和 20% 峰值容量裕度 QA。结果属于 `weighted_period_test`，采用固定 COP、临时经济参数和非道路几何 MST，不是正式年度或施工方案。
- [ ] 先选 6～12 栋代表建筑和连续 168 小时进行端到端调试。
- [ ] 再扩展到园区全部有效建筑。
- [ ] 建立全分散、组团微网和集中供热三个基线方案。
  - V0 接通记录（2026-08-23）：8 栋峰值日已由同一 Core 分别求解 `central/distributed/hybrid`，并执行统一 20% 峰值容量裕度；这只是固定 COP、临时参数和几何 MST 的调试结果，尚不能勾选正式基线任务。
- [ ] 进行至少一次聚类数量敏感性分析。
- [ ] 对比建筑级负荷总和、聚类后负荷总和和模型满足负荷，误差不超过 0.1%。
- [ ] 形成“哪些结果可信、哪些仍由假设驱动”的清单。

### 工作包 D：模型竞赛化扩展

交付物：设备技术表、碳排模块、柔性模块、单元测试和公式说明。

- [ ] 将设备参数从散落的列升级为明确的 `technology_id` 数据结构。
- [ ] 先实现固定 COP 热泵，再实现二维/三维性能曲线插值或分段线性化。
- [ ] 加入电、气和余热的逐时成本与碳排。
- [x] 用 epsilon-constraint 或权重扫描生成 Pareto 点，不只给一个加权结果。
  - 完成记录（2026-08-21）：集中式、分布式和混合式分别从成本/碳端点执行 epsilon-constraint，清理重复/支配点并标记膝点；每个点重新建立干净模型，标准结果目录保留完整设备、连接、网络、调度、成本、碳排和 QA 文件。
- [ ] 先接入负荷组给出的柔性上下界；如建立 RC 模型，必须用 DeST 扰动工况校准。
- [ ] 对每项新增公式建立小规模解析或手算可核验测试。

### 工作包 E：最终区域迁移

交付物：武汉新城正式案例、场景矩阵、Pareto 结果、完整供暖季校核。

- [ ] 冻结研究边界、基准年、规划建筑和能源站候选点。
- [ ] 将软件园流程原样迁移，不复制粘贴临时代码。
- [ ] 使用代表时段完成规划求解，再对候选投资方案做完整供暖季逐时运行校核。
- [ ] 输出空间布局、设备容量、管网长度、能源结构、成本、碳排、峰值电力和未供热量。
- [ ] 对负荷、价格、COP、碳因子和建设规模做敏感性分析。

---

## 6. 与负荷组的正式对接机制

### 6.1 负荷组交给代码组

| 文件 | 内容 | 代码组接收检查 |
|---|---|---|
| `01_archetypes.csv` | DeST 原型元数据和年度指标 | `archetype_id` 唯一，单位齐全 |
| `02_archetype_hourly_loads.parquet` | 原型逐时冷热负荷 | 时间连续，无空值，年度累计可对账 |
| `03_buildings.geojson` | 建筑轮廓及清单 | `building_id` 唯一，Geometry 有效，CRS 明确 |
| `04_building_archetype_map.csv` | 建筑—原型映射、缩放系数和置信度 | 每栋建筑恰有一个主映射；低置信度可追踪 |
| `05_building_hourly_loads.parquet` | 建筑级逐时负荷标准长表 | ID 全匹配，单位为 kW，时间戳统一 |
| `06_equipment_performance.csv` | 设备性能曲线 | 自变量范围、单位、来源和假设标记完整 |
| `07_data_dictionary.md` | 字段与单位定义 | 与实际文件一致 |
| `08_qa_report.xlsx` | 行数、缺失、峰值、累计和异常说明 | 关键检查全部通过 |
| `09_source_log.csv` | 数据来源与处理记录 | 来源、日期、许可和负责人可追溯 |

负荷组应以 `building_id` 为跨文件主键。任何文件更新都要附版本号或日期，不覆盖旧版本且不说明变化。

### 6.2 代码组在收到数据后 24 小时内反馈

代码组不能只回复“收到”或“跑不了”，应返回以下内容：

| 反馈文件 | 必须包含 |
|---|---|
| `interface_acceptance.md` | 接收文件版本、字段检查、单位判断、缺失问题和是否准入 |
| `load_reconciliation.csv` | 每栋建筑转换前后累计热量、峰值和误差 |
| `id_mismatch.csv` | 仅出现在某一文件中的 ID；无异常时也保留空表 |
| `adapter_run.log` | 转换命令、环境版本、随机种子和输出路径 |
| `questions_for_load_team.md` | 只列必须由负荷组回答的明确问题，不写笼统描述 |

### 6.3 对接边界

负荷组负责：

- DeST 原型、建筑用途映射、建筑面积缩放和逐时负荷；
- 设备性能原始曲线及其来源；
- 低置信度建筑和参数上下界；
- 负荷侧舒适性或柔性工况。

代码组负责：

- 数据契约、校验器、格式适配和单位换算；
- 聚类、管网候选、设备选型和优化模型；
- 价格、碳排、网络、储热和求解器接口；
- 情景管理、结果对账、可视化和可重复运行。

共同确认：

- 供热负荷是否包含生活热水；
- 时间采用自然年、典型气象年还是某一实测年；
- 闰年如何处理；
- 冷负荷是否只用于综合能源扩展，不进入当前供热模型；
- 负荷单位和正负号；
- 设备性能曲线的外推规则。

---

## 7. 数据接口契约

### 7.1 建议保留的标准长表

`05_building_hourly_loads.parquet`：

| 字段 | 类型 | 单位/规则 |
|---|---|---|
| `timestamp` | datetime | 小时起点，统一时区；不得重复 |
| `building_id` | string | 与 GeoJSON 完全一致 |
| `heating_kW` | float | 非负；本竞赛当前模型主要使用 |
| `cooling_kW` | float | 非负；暂不进入原始 UrbanHeatOpt，但保留 |
| `data_version` | string | 对应负荷组交付版本 |
| `quality_flag` | string | `measured/simulated/scaled/estimated` 等 |

标准长表是项目主数据；UrbanHeatOpt 宽表只是适配产物，不得反过来成为唯一数据源。

### 7.2 当前 UrbanHeatOpt 兼容宽表

`Building_TS.csv`：

```csv
hour,building_0001,building_0002,building_0003
1,12.45,8.16,0.00
2,11.92,7.88,0.00
```

目标约定：

- `hour` 为 `1...N`，与现有储热时序约束兼容；
- 建筑列名必须与 `Building_Data.geojson` 中的 `building_id` 完全一致；
- 数值明确为 `kW`；
- 代码中不再进行来源不明的 `/1000`；
- 如果为调试只使用 24 或 168 小时，文件名、配置和报告必须明确标注，不得冒充全年结果。

兼容旧代码时，可以在适配器中临时输出“Wh/小时步长”，但必须由配置显式声明，并用测试确认只换算一次。

### 7.3 `Building_Data.geojson` 最低字段

| 字段 | 单位 | 用途 |
|---|---:|---|
| `building_id` | — | 与负荷列关联 |
| `geometry` | WGS84 GeoJSON | 建筑轮廓；代码内部再投影 |
| `heated_area` | m² | 连接成本和统计 |
| `number_of_dwellings` | 户 | 仅在启用生活热水时使用；非住宅可为 0 |
| `YearlyDemand` | kWh/a | 建筑年度供热量 |
| `MaxDemand` | kW | 容量上限与连接功率 |
| `LocalHeatProdCosts` | 元/kWh 或统一币种/kWh | 分散供热基线成本 |
| `GeneralisedThermCap` | kWh/K | 原热状态模型参数；不用时仍需说明来源 |
| `GeneralisedThermCond` | kW/K | 原热状态模型参数；不用时仍需说明来源 |

建议同时保留 `use_type`、`floors`、`archetype_id`、`scale_factor`、`confidence`、`source` 等追溯字段。输入读取函数必须只对“最低必需字段”执行非空检查，不能因辅助字段为空删除建筑。

### 7.4 `06_equipment_performance.csv` 建议字段

```text
technology_id,technology_type,T_out_C,T_supply_C,PLR,
COP,capacity_ratio,variable_cost_CNY_per_kWh_heat,
carbon_kgCO2e_per_kWh_heat,source,assumption_flag
```

对于燃气锅炉等不使用某些自变量的设备，可留空，但数据字典必须定义插值和缺失处理。代码组应将曲线预处理为模型能够接受的分段线性参数，并输出转换检查图。

### 7.5 必须自动检查的规则

- `timestamp × building_id` 唯一；
- 时间连续且所有建筑覆盖完全相同的小时；
- 负荷、面积、容量和成本非负；
- GeoJSON CRS 可识别，面积和距离不得直接在经纬度上计算；
- 建筑 ID 集合在 GeoJSON、映射表和负荷表中一致；
- `sum(building loads) == sum(cluster loads)`，误差不超过 0.1%；
- 年度累计值与负荷组 QA 报告一致；
- 热源名称与性能曲线、价格表和时序列名一致；
- 设备曲线不得在未告警的情况下超范围外推。

---

## 8. 输入适配器的验收标准

建议命令：

```powershell
conda activate urbanheatopt_env
python scripts/validate_inputs.py --case cases/guanggu_software_park
python scripts/adapt_load_team_data.py --case cases/guanggu_software_park
pytest -q
```

适配器通过的必要条件：

- [ ] 两次运行生成相同文件校验值或等价数值结果。
- [ ] 所有单位换算在日志中只出现一次。
- [ ] 输出宽表行数、列数和 ID 集合正确。
- [ ] 建筑年度负荷转换误差 ≤ 0.1%。
- [ ] 遇到错位 ID、重复时间、未知单位或非法几何时明确失败。
- [ ] 不修改负荷组交付的原始文件。

---

## 9. 优化模型的竞赛化路线

### 9.1 第一层：先保留原模型，得到可解释基线

原模型已经具备：建筑聚类、热网投资、集中/分散供热选择、热源容量投资、储热、未供热惩罚和成本最小化。先用它回答：

- 哪些建筑组团适合联网？
- 集中与分散供热的经济边界是什么？
- 热源容量和储热容量如何选择？
- 管网长度和连接成本如何影响布局？

### 9.2 第二层：增加高品质供热所需能力

建议的统一设备表至少包含：

| 类别 | 必要参数 |
|---|---|
| 空气源热泵 | COP/容量随室外温度、供水温度、PLR 的变化，除霜修正 |
| 水源/地源热泵 | 源侧温度、可用热量、COP、取热限制 |
| 余热热泵 | 余热温度和时序、提升温差、COP、可利用比例 |
| 电锅炉 | 效率、容量、分时电价和电力碳因子 |
| 燃气锅炉 | 效率、燃气价格和排放因子 |
| 储热 | 容量、充放功率、效率、损失率、首末状态 |

供水温度应成为场景参数，至少比较 35/45/55 ℃ 等低温供热方案；设备 COP、管网损失和末端适配必须与该温度一致。

### 9.3 第三层：成本—碳排多目标

建议先分别求：

1. 最小年化成本；
2. 最小全生命周期或运行碳排；
3. 用碳排上限逐步收紧的 epsilon-constraint 场景。

不要只给任意权重的单个加权和。每个 Pareto 点应输出：

- 年化总成本及构成；
- 碳排及构成；
- 各设备容量与年运行量；
- 管网长度和联网建筑；
- 峰值电力需求；
- 储热循环；
- 未供热量和舒适性违约。

### 9.4 第四层：负荷柔性与室温

推荐逐级实现：

1. 固定 DeST 基线负荷；
2. 负荷组提供的逐时上下界与可转移比例；
3. 预热/降温工况的能量守恒窗口；
4. 经 DeST 工况校准的 RC 室温模型。

如果 DeST 负荷已经体现某种控制策略，不得在优化模型中重复计算同一部分柔性。

### 9.5 时间尺度策略

- 24～48 小时只用于单元和冒烟测试；
- 连续 168 小时用于流程调试；
- 代表日/代表周用于投资优化时，应具有逐时权重；
- 储热状态不能在不连续代表日之间错误串联；
- 最终候选投资方案必须在完整供暖季或 8760 小时上进行运行回放。

---

## 10. 光谷软件园试点的方案矩阵

首轮不要设置过多变量，建议固定为下表：

| 场景 | 管网 | 热源 | 储热 | 用途 |
|---|---|---|---|---|
| S0 | 无 | 建筑分散热泵 | 无 | 分散基线 |
| S1 | 组团 | 组团空气源热泵 | 可选 | 验证聚类与微网 |
| S2 | 区域 | 集中空气源热泵 + 电锅炉备用 | 有 | 验证源网荷储闭环 |
| S3 | 区域 | 热泵 + 假设余热源 + 备用热源 | 有 | 验证多热源调度；余热必须标明为情景假设 |

敏感性分析至少包括：

- 建筑用途/面积误差；
- 聚类数量；
- 供水温度；
- 热泵 COP；
- 电价与碳因子；
- 管网单位造价；
- 储热造价；
- 余热可利用量。

---

## 11. 质量门槛与“完成”的定义

### 11.1 数据层

- [ ] 有数据字典、来源日志和版本号。
- [ ] 所有单位在机器可读配置中声明。
- [ ] 输入文件由校验器通过，不能靠人工目测。
- [ ] 负荷转换与聚类前后守恒误差 ≤ 0.1%。

### 11.2 模型层

- [ ] 求解状态为 optimal，或明确记录 gap 和停止原因。
- [ ] 未供热量为 0，或给出经过批准的上限和原因。
- [ ] 每小时节点能量/质量平衡残差在数值容差内。
- [ ] 热源输出不超过容量和可用曲线。
- [ ] 储热首末状态、充放功率、容量和损失均满足约束。
- [ ] 选择的管道与实际流量一致。
- [ ] 成本和碳排可以从明细重新求和得到总值。

### 11.3 工程层

- [ ] 新环境按说明可以安装和运行。
  - 当前状态（2026-08-11）：`environment.yml` 的 Conda 求解部分仅使用 conda-forge，另保留 4 个显式 PyPI 依赖；全新环境 dry-run 已成功，但尚未实际创建第二个隔离环境，因此保持未勾选。
- [x] 非 Notebook 命令行入口可用。
- [x] 小型测试数据随代码提供。
- [x] 固定随机种子后结果可重现。
  - V0 验证记录（2026-08-21）：随仓库提供 `tests/fixtures/v3_smoke_case/`，全部值标记为 `synthetic_test`；单命令双跑和输入只读回归通过。正式数据与完整季仍未验收。
- [ ] 测试通过后才生成正式图表。
- [ ] 代码修改、公式、假设和数据版本均有记录。

### 11.4 竞赛表达层

- [ ] 可以从建筑地图追溯到负荷、设备和方案选择。
- [ ] 有基线方案，不只展示“最优方案”。
- [ ] 有成本—碳排 Pareto，而非单一主观权重。
  - V0 验证记录（2026-08-21）：三模式与合并 Pareto 已输出；由于尚未计入泵耗、管损和温度相关 COP，正式竞赛表达门槛保持未勾选。
- [ ] 有不确定性和敏感性分析。
- [ ] 明确哪些数据真实、哪些来自 DeST、哪些是情景假设。

---

## 12. 建议里程碑

| 时间 | 代码组必须完成 | 与负荷组的接口节点 |
|---|---|---|
| 第 1～2 天 | Git 基线、求解器、最小合成案例、输入校验器 | 双方冻结 ID、时间和单位 |
| 第 3 天 | 长表转宽表适配器、GeoJSON 模型字段适配 | 用负荷组最小样例完成第一次接收测试 |
| 第 4～5 天 | 6～12 栋建筑、168 小时端到端求解 | 返回对账表与问题清单 |
| 第 6～7 天 | 软件园首个全区域基线方案 | 双方联合核对区域总负荷和峰值 |
| 第 2 周 | 设备曲线、碳排、储热和柔性接口 | 负荷组补充设备/柔性参数 |
| 第 3～4 周 | 最终区域迁移、Pareto 和敏感性分析 | 冻结最终数据版本和假设范围 |

---

## 13. 给代码组 Codex 的执行协议

Codex 开始工作前应执行：

1. 阅读本文档和仓库 `README.md`、`_config.yaml`。
2. 检查当前工作树，不覆盖学生已有修改。
3. 只选择“第一个尚未完成且没有外部依赖”的任务。
4. 先复现问题或建立失败测试，再修改代码。
5. 每次修改都运行相关测试和一个最小案例。
6. 不编造负荷、设备价格、碳因子或地图数据；缺失参数写入假设文件。
7. 不把大规模求解成功等同于模型正确，必须输出守恒与边界检查。
8. 完成任务后更新本文档复选框，并写明文件、命令和验证结果。

### 可直接复制给 Codex 的首次指令

```text
请先完整阅读 CODE_TEAM_ROADMAP.md、README.md、_config.yaml，以及与第一个 P0 任务直接相关的代码。
不要立即运行大规模案例，也不要修改或猜测负荷数据。

请完成第一个尚未勾选、且无外部依赖的 P0 任务：
1. 先报告你核实到的当前状态和风险；
2. 实施最小、可回退的修改；
3. 添加自动测试或可重复的检查命令；
4. 运行验证；
5. 更新 CODE_TEAM_ROADMAP.md 中对应复选框，并记录结果。

如果任务需要负荷组补充信息，请不要自行假设；把问题写入
questions_for_load_team.md，同时继续完成不依赖该信息的部分。
```

### 后续每次交给 Codex 的指令模板

```text
继续执行 CODE_TEAM_ROADMAP.md。
读取上一次提交与测试结果，选择下一个未完成且无阻塞的任务。
保持数据接口向后兼容；如必须改变接口，同时更新适配器、测试、
DATA_CONTRACT.md 和 CHANGELOG_COMPETITION.md。
完成后运行测试和最小案例，并用结果更新任务书。
```

---

## 14. 代码组下一次会议必须回答的问题

1. 当前能否用**随仓库分发的最小样例**从零执行到 optimal？
2. `Building_TS.csv` 的单位、小时编号和建筑 ID 是否已经由测试冻结？
3. 负荷组数据经过适配后，年度热量和峰值误差是多少？
4. 当前场景是否重复加入了生活热水？
5. 当前解中未供热量是否为 0？
6. 当前热泵是否真的使用 COP 曲线，还是仍被当作固定成本 boiler？
7. 当前管网是空间直线候选还是道路可实施候选？
8. 哪些输入来自公开/模拟数据，哪些只是暂定假设？
9. 下一项最影响竞赛结论可信度的模型扩展是什么？

如果以上问题无法用文件、测试或输出表回答，则该环节尚未完成。
