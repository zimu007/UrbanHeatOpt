# 负荷组待确认问题

> 状态：2026-08-11；当前未接收正式九文件。本清单只列真实数据接入前必须得到明确答案的问题，不阻塞合成 P0。

## A. 版本与来源

1. 本次九份交接文件各自的 `data_version`、生成日期和 SHA-256 是什么？相对上一版本分别改了什么？
2. `09_source_log.csv` 是否能逐文件给出来源、许可、责任人和处理步骤，并关联到本次精确版本？
3. `07_data_dictionary.md` 是否覆盖实际每一列的类型、单位、可空性、枚举、符号、时区和缺失处理？

## B. 时间与负荷语义

4. 时间采用自然年、典型气象年还是实测年？正式供暖季的起点和终点是什么？
5. 时区是否为 `Asia/Shanghai`？`timestamp` 是否表示小时区间起点？是否全部位于整点？
6. 闰年如何处理？所有原型和建筑是否覆盖完全相同、连续且无重复的小时集合？
7. `heating_kW` 和 `cooling_kW` 是否都是一小时平均功率、单位 kW、供热/供冷均以非负值表示？
8. `05_building_hourly_loads.parquet` 能否增加必需布尔列 `dhw_included`？该状态是全案例一致，还是可能逐栋或逐时变化？
9. 建筑负荷是否已经完成一次且仅一次面积缩放？请给出 `scale_factor` 公式、原型基准面积和目标 `heated_area_m2`。
10. `cooling_kW` 是否确认只保留给后续综合能源扩展，而不进入当前供热 P0？

## C. 建筑、原型与 DeST

11. `heated_area_m2` 的定义和来源是什么？是否明确不是建筑占地面积？
12. `use_type`、`archetype_id`、`quality_flag` 和映射置信度分别允许哪些值？低置信度建筑如何处理？
13. DeST 使用的室内设定温度、气象文件、控制策略和典型年/年份版本是什么？
14. 负荷是否已经体现预热、降温或其他柔性控制？若是，优化模型可使用的柔性上下界是什么，如何避免重复计算？
15. `08_qa_report.xlsx` 的工作表、峰值和累计计算口径、验收容差及异常关闭标准是什么？

## D. 设备与资源

16. `06_equipment_performance.csv` 各温度、PLR、COP、容量比、成本和碳因子的单位与有效范围是什么？
17. 曲线点间采用什么插值？超出有效范围时是禁止、截断还是允许有依据的外推？缺失值如何处理？
18. 设备容量上下限、投资、固定/可变运维、寿命、价格基年和含税口径由负荷组还是项目组提供？对应来源是什么？
19. 若启用余热、污水或河湖资源，谁提供资源位置、温度、逐时可利用热功率、取用限制和许可条件？
20. 正式室外温度、电价、燃气价、电力/燃气碳因子由谁提供？版本、时间范围和能源热值口径是什么？

收到答案后，应逐项关闭问题并记录回答人、日期和对应数据版本；不得用口头约定覆盖版本化文件。
