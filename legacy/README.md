# 历史参考，不是默认运行入口

upstream保存原作者模块及演示；documentation保存旧网站与早期开发记录。旧回归入口位于tools/legacy_cli/。这些内容不能作为新数据已接通或V2已完成的证明。

新代码不得隐式import legacy。历史代码仍保留原参数/算法边界，不能把旧回归输出称为新经济包结果。default、Fehring和旧运行目录原位保留，避免移动用户数据。
